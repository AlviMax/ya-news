# news/pytest_tests/test_logic.py
"""
Тесты логики работы с комментариями на pytest.

Проверяют:
- Создание комментариев авторизованными пользователями.
- Запрет создания комментариев анонимными пользователями.
- Блокировку запрещённых слов в комментариях.
- Редактирование комментариев только автором.
- Удаление комментариев только автором.
"""
import pytest
from http import HTTPStatus

from news.models import Comment
from news.forms import BAD_WORDS, WARNING


@pytest.mark.django_db
class TestCommentCreation:
    """
    Тестирование создания комментариев.

    Проверяет три сценария:
    1. Анонимный пользователь не может создать комментарий.
    2. Авторизованный пользователь может создать комментарий.
    3. Авторизованный пользователь не может использовать запрещённые слова.
    """

    def test_anonymous_user_cant_create_comment(
            self, client, detail_url, form_data
    ):
        """
        Назначение теста 9.

        Проверяет, что анонимный пользователь не может создать комментарий.

        Шаги теста:
        1. Отправляем POST-запрос на страницу новости с данными комментария
           от анонимного клиента (без авторизации).
        2. Проверяем, что комментарий не появился в базе данных.

        Ожидаемый результат:
            Комментарий не создаётся, количество комментариев в базе равно 0.
        """
        # Отправляем POST-запрос от анонимного пользователя
        client.post(detail_url, data=form_data)

        # Проверяем, что комментарий НЕ создался
        assert Comment.objects.count() == 0

    def test_user_can_create_comment(
            self, client, author, detail_url, form_data, news
    ):
        """
        Назначение теста 10.

        Проверяет, что авторизованный пользователь может создать комментарий.

        Шаги теста:
        1. Авторизуем пользователя в тестовом клиенте.
        2. Отправляем POST-запрос на страницу новости с данными комментария.
        3. Проверяем, что произошёл редирект на страницу новости detail.html
           с якорем #comments.
        4. Проверяем, что в базе данных появился ровно один комментарий.
        5. Проверяем, что текст комментария соответствует отправленному.
        6. Проверяем, что комментарий привязан к правильной новости.
        7. Проверяем, что автором комментария является текущий пользователь.

        Ожидаемый результат:
            Комментарий успешно создаётся, все поля заполнены правильно,
            пользователь перенаправляется на страницу новости.
        """
        # Авторизуем пользователя
        client.force_login(author)

        # Отправляем POST-запрос на создание комментария
        response = client.post(detail_url, data=form_data)

        # Проверяем, что произошёл редирект (302)
        assert response.status_code == HTTPStatus.FOUND

        # Проверяем, что редирект ведёт на страницу detail.html
        # с якорем #comments
        assert response.url == f'{detail_url}#comments'

        # Проверяем, что комментарий появился в базе (1 запись)
        assert Comment.objects.count() == 1

        # Получаем созданный комментарий
        comment = Comment.objects.get()

        # Проверяем, что текст совпадает
        assert comment.text == form_data['text']

        # Проверяем, что комментарий привязан к правильной новости
        assert comment.news == news

        # Проверяем, что автор комментария — текущий пользователь
        assert comment.author == author

    def test_user_cant_use_bad_words(self, client, author, detail_url):
        """
        Назначение теста 11.

        Проверяет, что пользователь не может использовать запрещённые слова.

        Шаги теста:
        1. Авторизуем пользователя в тестовом клиенте.
        2. Формируем данные комментария, содержащие запрещённое слово
           (первое слово из списка BAD_WORDS).
        3. Отправляем POST-запрос на страницу новости.
        4. Проверяем, что в форме появилась ошибка в поле 'text'
           с текстом WARNING.
        5. Проверяем, что комментарий не был создан в базе данных.

        Ожидаемый результат:
            Комментарий с запрещённым словом не создаётся,
            пользователь видит сообщение об ошибке.
        """
        # Авторизуем пользователя
        client.force_login(author)

        # Формируем данные с запрещённым словом (первое из списка BAD_WORDS)
        bad_words_data = {
            'text': (
                f'Запрещенное слово:, {BAD_WORDS[0]}, '
                'не допустимо'
            )
        }

        # Отправляем POST-запрос
        response = client.post(detail_url, data=bad_words_data)

        # Получаем форму из контекста ответа
        form = response.context['form']

        # Проверяем, что в поле 'text' есть ошибка с текстом WARNING
        assert form.errors['text'] == [WARNING]

        # Проверяем, что комментарий НЕ создался
        assert Comment.objects.count() == 0


@pytest.mark.django_db
class TestCommentEditDelete:
    """
    Тестирование редактирования и удаления комментариев.

    Проверяет четыре сценария:
    1. Автор может удалить свой комментарий.
    2. Читатель не может удалить чужой комментарий.
    3. Автор может редактировать свой комментарий.
    4. Читатель не может редактировать чужой комментарий.
    """

    def test_author_can_delete_comment(
            self, client, author, delete_url, comment
    ):
        """
        Назначение теста 12.

        Проверяет, что автор может удалить свой комментарий.

        Шаги теста:
        1. Авторизуем автора комментария в тестовом клиенте.
        2. Отправляем DELETE-запрос на URL удаления комментария.
        3. Проверяем, что произошёл редирект (статус 302).
        4. Проверяем, что комментарий исчез из базы данных.

        Ожидаемый результат:
            Комментарий успешно удаляется, база данных становится пустой.
        """
        # Авторизуем автора
        client.force_login(author)

        # Отправляем DELETE-запрос на удаление
        response = client.delete(delete_url)

        # Проверяем, что произошёл редирект (302)
        assert response.status_code == HTTPStatus.FOUND

        # Проверяем, что комментарий удалён из базы
        assert Comment.objects.count() == 0

    def test_user_cant_delete_comment_of_another_user(
            self, client, reader, delete_url, comment
    ):
        """
        Назначение теста 13.

        Проверяет, что пользователь не может удалить чужой комментарий.

        Шаги теста:
        1. Авторизуем читателя (другого пользователя) в тестовом клиенте.
        2. Отправляем DELETE-запрос на URL удаления комментария.
        3. Проверяем, что вернулся статус 404 (Not Found).
        4. Проверяем, что комментарий остался в базе данных.

        Ожидаемый результат:
        Чужой комментарий не удаляется, возвращается ошибка 404,
        комментарий остаётся в базе.
        """
        # Авторизуем читателя
        client.force_login(reader)

        # Пытаемся удалить чужой комментарий
        response = client.delete(delete_url)

        # Проверяем, что вернулась ошибка 404 (Not Found)
        assert response.status_code == HTTPStatus.NOT_FOUND

        # Проверяем, что комментарий остался в базе
        assert Comment.objects.count() == 1

    def test_author_can_edit_comment(
            self, client, author, edit_url, comment, update_data
    ):
        """
        Назначение теста 14.

        Проверяет, что автор может редактировать свой комментарий.

        Шаги теста:
        1. Авторизуем автора комментария в тестовом клиенте.
        2. Отправляем POST-запрос на URL редактирования комментария
           с обновлёнными данными.
        3. Проверяем, что произошёл редирект (статус 302).
        4. Обновляем объект комментария из базы данных.
        5. Проверяем, что текст комментария изменился на новый.

        Ожидаемый результат:
        Комментарий успешно обновляется, текст комментария меняется.
        """
        # Авторизуем автора
        client.force_login(author)

        # Отправляем POST-запрос с обновлёнными данными
        response = client.post(edit_url, data=update_data)

        # Проверяем, что произошёл редирект (302)
        assert response.status_code == HTTPStatus.FOUND

        # Обновляем объект комментария из базы данных
        comment.refresh_from_db()

        # Проверяем, что текст комментария обновился
        assert comment.text == update_data['text']

    def test_user_cant_edit_comment_of_another_user(
            self, client, reader, edit_url, comment
    ):
        """
        Назначение теста 15.

        Проверяет, что пользователь не может редактировать чужой комментарий.

        Шаги теста:
        1. Авторизуем читателя (другого пользователя) в тестовом клиенте.
        2. Отправляем POST-запрос на URL редактирования чужого комментария
           с новыми данными.
        3. Проверяем, что вернулся статус 404 (Not Found).
        4. Обновляем объект комментария из базы данных.
        5. Проверяем, что текст комментария НЕ изменился.

        Ожидаемый результат:
        Чужой комментарий не редактируется, возвращается ошибка 404,
        текст комментария остаётся прежним.
        """
        # Авторизуем читателя
        client.force_login(reader)

        # Пытаемся отредактировать чужой комментарий
        response = client.post(edit_url, data={'text': 'Новый текст'})

        # Проверяем, что вернулась ошибка 404 (Not Found)
        assert response.status_code == HTTPStatus.NOT_FOUND

        # Обновляем объект комментария из базы данных
        comment.refresh_from_db()

        # Проверяем, что текст НЕ изменился
        assert comment.text == 'Текст комментария'
